Review the current git diff and provide feedback.

Focus on:
1. Type safety — are there any `any` types or unsafe casts?
2. React patterns — proper use of hooks, memoization where needed
3. Accessibility — semantic HTML, ARIA attributes, keyboard navigation
4. Test coverage — are new components tested?

Format: list issues as `file:line — description`. End with a summary.
